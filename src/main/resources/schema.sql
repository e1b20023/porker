CREATE TABLE poker2 (
    id IDENTITY,
    num INT NOT NULL,
    suit VARCHAR NOT NULL
);

CREATE TABLE poker3 (
    id IDENTITY,
    num INT NOT NULL,
    suit VARCHAR NOT NULL
);

CREATE TABLE poker4 (
    id IDENTITY,
    num INT NOT NULL,
    suit VARCHAR NOT NULL
);
